services:
  - type: web_service
    name: unitrans-backend
    env: python
    plan: starter
    branch: main
    root: backend-api
    buildCommand: pip install -r requirements.txt && python manage.py migrate && python manage.py collectstatic --noinput
    startCommand: gunicorn unitrans.wsgi:application --bind 0.0.0.0:$PORT
    autoDeploy: true
