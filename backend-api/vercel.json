{
  "buildCommand": "uv pip install --system -r requirements.txt && python manage.py collectstatic --noinput",
  "outputDirectory": "staticfiles",
  "installCommand": "uv pip install --system -r requirements.txt"
}
